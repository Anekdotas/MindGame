package anekdotas.mindgameapplication.objects

import anekdotas.mindgameapplication.network.HostModel

object HostObject {
    val host = HostModel(listOf("good", "acceptable", "nice"), listOf("incorrect!", "very bad"))
}